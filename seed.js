// Este arquivo NÃO é mais necessário — a partir da v2.1, o próprio
// server/index.js cria as tabelas e popula os dados sozinho, toda vez
// que o servidor sobe no Render (é seguro, nunca duplica nada).
// Mantido aqui só como referência, caso um dia você queira rodar
// esse mesmo seed manualmente em outro banco.
console.log('Não precisa rodar este arquivo — o index.js já faz isso sozinho ao iniciar.');
